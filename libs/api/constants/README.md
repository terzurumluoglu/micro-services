# Api Constants

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build api-constants` to build the library.

## Running unit tests

Run `nx test api-constants` to execute the unit tests via [Jest](https://jestjs.io).
