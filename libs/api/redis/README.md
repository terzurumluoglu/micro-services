# api-redis

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build api-redis` to build the library.

## Running unit tests

Run `nx test api-redis` to execute the unit tests via [Jest](https://jestjs.io).
