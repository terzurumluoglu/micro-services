# Api Factories

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build api-factories` to build the library.

## Running unit tests

Run `nx test api-factories` to execute the unit tests via [Jest](https://jestjs.io).
