# api-platform

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build api-platform` to build the library.

## Running unit tests

Run `nx test api-platform` to execute the unit tests via [Jest](https://jestjs.io).
